# Lê um número inteiro fornecido pelo usuário e armazena na variável 'a'
a = int(input())
# Lê um número inteiro fornecido pelo usuário e armazena na variável 'b'
b = int(input())

# Calcula a soma de 'a' e 'b' e imprime no formato solicitado
# Usando f-string para inserir o resultado dentro da string
print(f"SOMA = {a + b}")
